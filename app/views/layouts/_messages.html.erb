<% flash.each do |key, value| %>

    <% if value != 'Signed in successfully.' && value != 'Signed out successfully.' && value != 'Welcome! You have signed up successfully.' %>

    <div class=" text-center alert alert-<%= key %>">
      <%= value %>
      <a href="#" data-dismiss="alert" class="close">×</a>
    </div>

    <% end %>
<% end %> 
